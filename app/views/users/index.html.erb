<h1>Users</h1>
<%= render @users %>
<% if @users.length < 1 %>
  <div class="box">
    <h2>No users have been created yet.</h2>
  </div>
<% end %>
<%= link_to 'Create User', '/user/new', :class => 'button' %>
